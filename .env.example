# Admin credentials (change before production demo)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Session encryption key — use a random string of at least 32 characters
SESSION_SECRET=change-me-to-a-random-string-at-least-32-chars

# SQLite storage directory (absolute or relative to project root)
DATA_DIR=data
